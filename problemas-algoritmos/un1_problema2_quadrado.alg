algoritmo "quadrado"
var
l, a, p : real

inicio
escreva("Informe o lado do quadrado: ")
leia(l)

a <- l * l
p <- l * 4

escreval(" A área do quadrado com lado ", l, " é igual a: ", a)
escreval(" O perimetro do quadrado com lado ", l, " é igual a: ", p)

fimalgoritmo
